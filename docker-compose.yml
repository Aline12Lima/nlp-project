services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./models:/app/models
    environment:
      - HF_HOME=/app/models
      - TRANSFORMERS_CACHE=/app/models
    env_file:
      - .env
    restart: unless-stopped